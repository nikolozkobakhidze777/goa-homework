// საკლასო დავალება:

// შექმენით მასივი, სადაც გექნებათ 1-დან 5-ის ჩათვლით და შემდეგ 5-დან 1-ის ჩათვლით რიცხვები.

// იპოვეთ 1-ის ინდექსი.
// იპოვეთ 1-ის ინდექსი, ოღონდ lastIndexOf მეთოდით.
// იპოვეთ 5-ის ინდექსი.
// იპოვეთ 5-ის ინდექსი, ოღონდ lastIndexOf მეთოდით.

let numbers = [1, 2, 3, 4, 5, 5, 4, 3, 2, 1];

let firstindex1 = numbers.indexOf(1);
console.log(firstIndex1);

let lastindex1 = numbers.lastIndexOf(1);
console.log(lastIndex1);

let firstIndex5 = numbers.indexOf(5);
console.log(firstIndex5);

let lastIndex5 = numbers.lastIndexOf(5);
console.log(lastIndex5);
